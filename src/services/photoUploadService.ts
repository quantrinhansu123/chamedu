/** Auto-stub: Firebase removed. Migrate to Supabase. */
import { notMigrated } from '../utils/notMigrated';

export const useNavigate = async (..._args: any[]): Promise<any> => { notMigrated('useNavigate'); };
export const useCheckIn = async (..._args: any[]): Promise<any> => { notMigrated('useCheckIn'); };
export const useCheckInHistory = async (..._args: any[]): Promise<any> => { notMigrated('useCheckInHistory'); };
export const uploadCheckInPhoto = async (..._args: any[]): Promise<any> => { notMigrated('uploadCheckInPhoto'); };
export const dataURLtoBlob = async (..._args: any[]): Promise<any> => { notMigrated('dataURLtoBlob'); };
